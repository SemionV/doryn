{
    "tasks": [
        {
            "type": "shell",
            "label": "Build testApp project with dependencies(debug)",
            "command": "sh",
            "args": [
                "build/build-local-dev-cmake.sh"
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "General build of the project"
        }
    ],
    "version": "2.0.0"
}