{
    "tasks": [
        {
            "type": "shell",
            "label": "Build testApp project with dependencies(debug)",
            "command": "make",
            "args": [
                "-f",
                "build/makefile",
                "build-test-app",
                "config=debug"
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "General build of the project"
        }
    ],
    "version": "2.0.0"
}